# Entirely optional. Every credential here can be typed on the settings page instead,
# and anything saved there wins over this file from then on - these values only ever
# seed a deployment that has never had one set. Copy to .env beside this file to use it.

ICLOUD_EMAIL=
ICLOUD_APP_PASSWORD=
AEROAPI_KEY=
ANTHROPIC_API_KEY=
PUSHOVER_TOKEN=
PUSHOVER_USER_KEY=
