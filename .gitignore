.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Secrets and runtime state — never commit these.
.env
data/
credentials.json
token.json

# Local overrides of the shipped example.
docker-compose.override.yml

# Claude Code session workspaces.
.claude/
