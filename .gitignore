.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Secrets and runtime state — never commit these.
.env
data/
credentials.json
token.json

# The stylesheet is built from styles/ and committed; its toolchain is not.
node_modules/

# Local overrides of the shipped example.
docker-compose.override.yml

# Claude Code session workspaces, and the screenshots its browser writes.
.claude/
.playwright-mcp/

# macOS Finder metadata.
.DS_Store
